<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Quiz</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel='stylesheet' href='/stylesheets/style.css'/>
    <link rel='stylesheet'
          type='text/css'
          media='only screen and (min-width: 800px) and (min-device-width: 600px)'
          href='/stylesheets/wide.css'/>
    <link rel='stylesheet'
          type='text/css'
          media='only screen and (max-width: 480px)'
          href='/stylesheets/smartphone.css'/>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bulma/0.7.5/css/bulma.min.css">
</head>

<body class="body">
<header class="logo">
    <a href="/" class="logolink">The Quiz Site</a>
</header>
<div>

</div>
<nav class="navbar" role="navigation" aria-label="main navigation">
      <div id="navMenu" class="navbar-menu">

        <div class="navbar-start">
            <a class="navbar-item" href="/">Home</a>
            <a class="navbar-item" href="/quizzes/random_play">Play</a>

            <div class="navbar-item has-dropdown is-hoverable">
                <a class="navbar-link" href="/users">Users</a>
                <div class="navbar-dropdown is-boxed">
                    <% if (!session.user){ %>
                        <a class="navbar-item" href="/login">LogIn</a>
                        <a class="navbar-item" href="/signup">SignUp</a>
                    <% }else{ %>
                        <a class="navbar-item" href="/users/<%= session.user.id %>" ><%= session.user.username%></a>
                        <a class="navbar-item" href="/logout">LogOut</a>
                    <% } %>
                </div>
            </div>
            <a class="navbar-item" href="/author">Author</a>
        </div>
    </div>
</nav>
<section class="hero">
    <% if (messages.info) { %>
        <div class='hero is-info'>
            <h3 class="subtitle">Info:</h3>
            <ul>
                <% for (var i in messages.info) { %>
                    <li> <%= messages.info[i] %> </li>
                <% } %>
            </ul>
        </div>
    <% } %>

    <% if (messages.success) { %>
        <div class='hero is-success'>
            <h3 class="subtitle">Tasks completed successfully:</h3>
            <ul>
                <% for (var i in messages.success) { %>
                    <li> <%= messages.success[i] %> </li>
                <% } %>
            </ul>
        </div>
    <% } %>

    <% if (messages.error) { %>
        <div class='hero is-danger'>
            <h3 class="subtitle">Errors:</h3>
            <ul>
                <% for (var i in messages.error) { %>
                    <li> <%= messages.error[i] %> </li>
                <% } %>
            </ul>
        </div>
    <% } %>

    <div class="hero-body">
        <%- body %>
        <% if (locals.paginate_control) { %>
            <%- paginate_control %>
        <% } %>
    </div>

</section>

</div>

<footer class="footer">
    <div class="box has-text-centered">
        <a class='subtitle is-6' href="http://en.wikipedia.org/wiki/Quiz">Would you like to know what a quiz is?</a>
    </div>
</footer>

</body>
</html>
